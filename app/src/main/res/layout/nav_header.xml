<!--Nave Header-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navHeaderRl"
    android:layout_width="match_parent"
    android:layout_height="215dp"
    android:background="@drawable/toolbar_bg"
    android:padding="20dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/navProfileIv"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="@color/colorPrimaryDark"
        android:padding="3dp"
        android:scaleType="centerCrop"
        app:shapeAppearanceOverlay="@style/ImageStyle_Circle"
        app:strokeColor="@color/material_on_primary_emphasis_medium"
        app:strokeWidth="3dp"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/navNameTv"
        style="@style/TextAppearance.Material3.BodyLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/navProfileIv"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="@tools:sample/full_names" />

    <TextView
        android:id="@+id/navEmailTv"
        style="@style/TextAppearance.Material3.BodySmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/navNameTv"
        android:textColor="@color/white"
        android:textSize="12sp"
        tools:text="abc@example.com" />

    <TextView
        android:id="@+id/navAccountTypeTv"
        style="@style/TextAppearance.Material3.LabelSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/navEmailTv"
        android:textColor="@color/white"
        android:textSize="12sp"
        tools:text="User" />

</RelativeLayout>